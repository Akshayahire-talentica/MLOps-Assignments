FROM apache/airflow:2.7.3-python3.9
# Pre-install all DAG-runtime dependencies so startup is fast
RUN pip install --no-cache-dir \
    mlflow==2.9.2 \
    boto3 \
    scikit-learn \
    pandas \
    numpy \
    evidently \
    great_expectations
